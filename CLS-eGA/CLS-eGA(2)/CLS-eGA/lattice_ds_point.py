class DSpoint:

    def __init__(self, latency, area, configuration,is_success=True):
        self.area = area
        self.latency = latency
        self.configuration = configuration
        self.is_success = is_success


